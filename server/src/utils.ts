import path from "path";

export const serverDirectory = path.join(path.resolve(__dirname), "../");
export const assetsDirectory = path.join(serverDirectory, "/assets");
export const audioDirectory = path.join(assetsDirectory, "/audio");
